# RentalCore Environment Configuration Template
# Copy this file to .env and customize the values for your environment

# =================================================================
# APPLICATION CONFIGURATION
# =================================================================
APP_PORT=8080
GIN_MODE=release

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# =================================================================
# DATABASE CONFIGURATION
# =================================================================
# External MySQL Database Settings
DB_HOST=your-mysql-host.com
DB_PORT=3306
DB_NAME=your-database-name
DB_USERNAME=your-db-username
DB_PASSWORD=your-secure-db-password

# Example for cloud databases:
# DB_HOST=mysql-db.amazonaws.com
# DB_HOST=your-edge-mysql.example.com

# =================================================================
# SECURITY CONFIGURATION
# =================================================================
# Generate a secure 32-character encryption key
# You can generate one with: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key-here

# Session timeout in seconds (default: 1 hour)
SESSION_TIMEOUT=3600

# =================================================================
# EMAIL CONFIGURATION (Optional)
# =================================================================
# SMTP settings for sending emails (invoices, notifications, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=RentalCore System
USE_TLS=true

# Leave empty to disable email functionality:
# SMTP_HOST=
# SMTP_USERNAME=
# SMTP_PASSWORD=

# =================================================================
# INVOICE CONFIGURATION
# =================================================================
# Default invoice settings
DEFAULT_TAX_RATE=19.0
DEFAULT_PAYMENT_TERMS=30
CURRENCY_SYMBOL=€
CURRENCY_CODE=EUR

# For different regions:
# US: CURRENCY_SYMBOL=$, CURRENCY_CODE=USD, DEFAULT_TAX_RATE=8.5
# UK: CURRENCY_SYMBOL=£, CURRENCY_CODE=GBP, DEFAULT_TAX_RATE=20.0

# =================================================================
# LOGGING CONFIGURATION
# =================================================================
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Available log levels: debug, info, warn, error
# For production, use 'info' or 'warn'
# For development/debugging, use 'debug'

# =================================================================
# NEXTCLOUD FILE POOL (Optional)
# =================================================================
NEXTCLOUD_WEBDAV_URL=
NEXTCLOUD_WEBDAV_USER=
NEXTCLOUD_WEBDAV_PASSWORD=
NEXTCLOUD_WEBDAV_BASE_PATH=rentalcore-filepool
FILEPOOL_ASSIGNED_ROOT=assigned
FILEPOOL_UNASSIGNED_ROOT=unassigned
# NEXTCLOUD_BACKFILL_ON_START=true

# =================================================================
# EXAMPLE CONFIGURATIONS FOR DIFFERENT ENVIRONMENTS
# =================================================================

# --- DEVELOPMENT EXAMPLE ---
# APP_PORT=8080
# GIN_MODE=debug
# LOG_LEVEL=debug
# DB_HOST=localhost
# ENCRYPTION_KEY=dev-key-32-chars-long-for-testing

# --- PRODUCTION EXAMPLE ---
# APP_PORT=8080
# GIN_MODE=release  
# LOG_LEVEL=info
# DB_HOST=prod-mysql.yourdomain.com
# ENCRYPTION_KEY=prod-secure-32-char-key-generated-securely

# =================================================================
# SECURITY NOTES
# =================================================================
# 1. NEVER commit your actual .env file to version control
# 2. Use strong, unique passwords for database access
# 3. Generate a unique ENCRYPTION_KEY for each deployment
# 4. Use HTTPS in production (configure via reverse proxy)
# 5. Regularly rotate passwords and keys
